Drop the Nothing Glyph Matrix Developer Kit AAR here (e.g. `glyph-matrix-sdk-2.0.aar`).

It's proprietary and Nothing-distributed, so it isn't committed to this repo.
`app/build.gradle.kts` references it by exact filename — update that line if yours differs.
